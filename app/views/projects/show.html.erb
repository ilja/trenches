<% title("#{@project.name}") %>
<header>
  <h1>
    <%= @project.name %>
  </h1>
  <nav class="header-right">
    <%= link_to "Manage project", edit_user_project_path(current_user, @project), :class => "button", :rel => "manage-project" %>
  </nav>
</header>

<div id="main" role="main">
  <%= render "shared/flash", :flash => flash %>

  <h2>Latest Sprint</h2>
  12-2-2012 - 13-3-2012
  Progress....
  <%= link_to "manage sprints", project_sprints_path(@project.owner_username, @project), :rel => "sprints" %>

  <h2><%= link_to "Product backlog", project_backlog_path(@project.owner_username, @project), :rel => "project-backlog" %></h2>
  top 10 stories uit backlog<br />


</div>
