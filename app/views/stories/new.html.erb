<header>
  <h1>
    <%= link_to @project.name, backlog_path(@project) %>
    /
    New Story
  </h1>
</header>

<div id="main" role="main">
  <%= render "shared/flash" %>

  <section class="backlog-container">
    <%= render 'form', :button_name => "Create Story" %>
  </section>

</div>
