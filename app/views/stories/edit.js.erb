$('.storybody').html("<%= escape_javascript(render :partial => 'form', :locals => { :button_name => "Update Story", :story => @story, :project => @project, :current_user => current_user, :remote => true }) %>");
